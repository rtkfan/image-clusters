[project]
name = "image-clusters"
version = "0.1.0"
description = "Cluster similar-looking images and extract a deduplicated set"
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = { file = "LICENSE.txt" }
dependencies = [
    "clustimage>=1.7.1",
]